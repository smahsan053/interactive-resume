{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2022" /* Set the JavaScript language version for emitted JavaScript. */,
    "module": "ESNext" /* Specify the module code generation. */,
    "moduleResolution": "node" /* Specify how TypeScript looks up a file from a given module specifier. */,
    "esModuleInterop": true /* Enable compatibility for importing CommonJS modules. */,
    "forceConsistentCasingInFileNames": true /* Ensure that casing is consistent in imports. */,

    /* Emit */
    "outDir": "./public/dist" /* Redirect output structure to this directory. */,
    "sourceMap": false /* Create source map files for emitted JavaScript files. */,
    "declaration": false /* Do not generate .d.ts files. */,
    "removeComments": true /* Disable emitting comments. */,

    /* Type Checking */
    "strict": true /* Enable all strict type-checking options. */,
    "skipLibCheck": true /* Skip type checking of all declaration files. */,

    /* Project */
    "rootDir": "./src" /* Specify the root folder within your source files. */,
    "declarationMap": false /* Do not create declaration maps. */,
    "downlevelIteration": true /* Emit more compliant, but verbose and less performant JavaScript for iteration. */,

    /* Modules */
    "resolveJsonModule": true /* Enable importing .json files. */
  },
  "include": ["src/**/*" /* Include all .ts files in the src directory. */],
  "exclude": ["node_modules" /* Exclude the node_modules directory. */]
}
